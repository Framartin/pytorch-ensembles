from torchvision import models

class cifar50cifar:
    base = models.
    args = list()
    kwargs = {'depth': 28, 'widen_factor': 10}


class WideResNet28x10do:
    base = WideResNet
    args = list()
    kwargs = {'depth': 28, 'widen_factor': 10, 'dropout_rate': 0.3}
